type Secret @entity {
  id: ID!
  text: String!
  likes: BigInt!
  reports: BigInt!
  created_at: BigInt!
}

type Like @entity {
  id: ID!
  address: Bytes!
  secret: Secret!
  status: Boolean!
}

type Report @entity {
  id: ID!
  address: Bytes!
  secret: Secret!
  status: Boolean!
}
